# Rust
/target
**/*.rs.bk

# Env / secrets
.env
*.local

# SQLx offline cache is committed intentionally (.sqlx/), but build artifacts aren't.

# Node / frontend (added in M6)
web/node_modules
web/dist
frontend/node_modules
frontend/dist
frontend/.vite

# Local dev run logs (backend/frontend launched by run.sh)
*.log

# OS
.DS_Store

# Claude Code runtime artifacts
.claude/scheduled_tasks.lock

# Deploy: local project config (the *.example template is committed)
deploy/config.env
deploy/secrets.env
